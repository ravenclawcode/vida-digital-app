import 'package:flutter/material.dart';

class QuestionTestPhqScreen extends StatelessWidget {
  const QuestionTestPhqScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
